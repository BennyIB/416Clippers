# MongoDB URI including the database name
spring.data.mongodb.uri=mongodb+srv://clippers:cse416clippers@416clippers.wz9ls42.mongodb.net/416Clippers?retryWrites=true&w=majority&appName=416Clippers

# Optional: Explicitly define the database name if URI does not work as expected
spring.data.mongodb.database=416Clippers

# Server port (default is 8080; you can change it if there's a port conflict)
server.port=8080

# Logging level (you can increase or decrease it based on your need for information)
logging.level.org.springframework.data=DEBUG
logging.level.org.springframework.web=DEBUG

# If using profiles, specify the active profile
spring.profiles.active=default

# Enable LiveReload support (useful during development)
spring.devtools.livereload.enabled=true
